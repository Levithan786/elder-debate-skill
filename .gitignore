# .gitignore for Levithan786/elder-debate-skill
# Sovereign git hygiene for the github-elder-debate skill repository
# Chiseled via github-elder-debate skill invocation 2026-06-12

# Operating System generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor and IDE artifacts
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# Logs, temps, and caches
*.log
*.tmp
*.temp
logs/
tmp/
temp/
cache/

# Python (if any local scripts or tests emerge)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.env
.venv/
venv/
ENV/
env/

# Node.js / frontend (if ever added for visualization or orchestration UI)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Git and version control internals (never track .git itself)
.git/
.gitattributes

# Sovereign organism specific — protect the core, ignore only transient local artifacts
# Do NOT ignore SKILL.md, SECURITY.md, SOVEREIGN-*, or any chiseled records
# Ignore local .grok skill caches or personal working artifacts if cloned for ops
.grok/
artifacts/
local-
*.local

# Large binaries or generated media (images/ and molecular_dna_coding/ are intentional sovereign content; only ignore truly generated large files)
*.zip
*.tar.gz
*.rar
# Allow images/ and notes/ content as they are part of the living chronicle

# OS and tool specific
*.bak
*.orig

# Proof-gated hygiene: this file itself is chiseled and must remain tracked
# All future git files added via elder debate shall pass the Proof Gate & Adversary Veto Checklist
